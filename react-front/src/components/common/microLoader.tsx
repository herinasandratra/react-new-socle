import './microLoader.css'; // Import the CSS file for styling

const MicroLoader = () => {
  return (
    <div className="micro-loader">
      <div className="spinner"></div>
    </div>
  );
};

export default MicroLoader;
